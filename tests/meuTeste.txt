int fatorial(int n){
    imprime(n);

    retorne n;
}

car global;

programa {
    int numeros[10], fatorialR;
    global = 'a';
    fatorialR = 2;

    numeros[0] = 10;
    numeros[1] = 20;
    numeros[2] = 30;

    imprime(global);
    imprime(fatorialR);
    imprime(numeros[2]);

    fatorialR = fatorial(numeros[0]);

    imprime(fatorialR);
}
